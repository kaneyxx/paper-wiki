"""Built-in plugin packages bundled with paper-wiki.

Each subpackage corresponds to one plugin role: ``sources``, ``filters``,
``scorers``, ``reporters``. External plugins published as separate
packages plug in via the entry-point groups defined in
:mod:`paperwiki.core.registry`.
"""

from __future__ import annotations
