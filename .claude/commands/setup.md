---
description: Verify paper-wiki environment and configure first-time setup
---

Invoke the paper-wiki setup skill.

If the environment is already configured (`~/.config/paperwiki/config.toml`
exists), summarize the current configuration and ask whether the user wants
to reconfigure. Otherwise, walk through fresh setup.
